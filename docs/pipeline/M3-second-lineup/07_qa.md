# M3 second-lineup 단계 7 QA

작성일: 2026-05-08.
검증 방식: 정적 시나리오 정합 + 단위 테스트 커버리지 + 사용자 라이브 시각 검증 (단계 7 권고).
단계 6 round 2 통과 결과 기반.

# 1. QA 목적

M3 다중 라인업 인프라 + 두 번째 라인업 (一番くじ ワンピース MONKEY.D.LUFFY) 추가 + M2.1 정리 라운드 흡수가 사용자 시나리오 (01_spec 6장 + 5.13.A) + 결정론을 정합하게 처리하는지 검증.

# 2. 정적 시나리오 정합

## 2.1. 시나리오 6.1 첫 진입 (드래곤볼 default, 신규 사용자)

| 단계 | 기대 | 정합 |
|---|---|---|
| 면책 안내 | DisclaimerSheet | OK |
| 헤더 라인업 IP 라벨 | "DRAGONBALL" | OK (header.js + .app-lineup-ip) |
| 추첨 탭 박스 카드 + 갤러리 | 드래곤볼 11등급 (A~J + Last One) | OK |
| 구매 패널 + skip 토글 OFF | BUY_SKIP_PICK_DEFAULT | OK |

## 2.2. 시나리오 5.13.A.4 라인업 전환 (드래곤볼 → 원피스, 사용자 결정 8.3 (A))

| 단계 | 기대 | 정합 |
|---|---|---|
| 설정 탭 'Lineup' dropdown 변경 | dispatch.set_current_lineup 트리거 | OK (settings-tab.js dropdown change) |
| 확인 모달 표시 | "라인업을 ... 전환합니다. 보존됩니다. 메모리 only 폐기됩니다." | OK (showConfirmModal) |
| 확인 시 currentLineupId 영속 | saveState({ currentLineupId }) | OK (P0 2.1 정정 후) |
| 새 라인업 공간 로드 | loadState → bootstrapState | OK |
| 메모리 only state 폐기 | pendingPeelResult / selectedGridIndices | OK (bootstrapState 재구성) |
| 헤더 라벨 갱신 | "ONE PIECE" | OK |
| 추첨 탭 갱신 | 원피스 10등급 (A~I + Last One) | OK |

## 2.3. 시나리오 5.13.A.2 데이터 격리 (드래곤볼 history 보존)

| 단계 | 기대 | 정합 |
|---|---|---|
| 드래곤볼에서 박스 진행 → 원피스 전환 → 드래곤볼 복귀 | 드래곤볼 박스 / 인벤토리 / 이력 / DC 보존 | OK (storage_v4 라인업별 격리 키) |
| 시드는 라인업 무관 공유 (사용자 결정 8.2 (A)) | 전역 키 kuji_seed | OK |
| skip_pick 라인업 무관 공유 | 전역 키 kuji_settings_skip_pick | OK |

## 2.4. 시나리오 7.17 마이그레이션 v3 → v4

| 단계 | 기대 | 정합 |
|---|---|---|
| v3 사용자 부팅 | LEGACY 키 → 드래곤볼 격리 키로 이전 + current_lineup_id default 부여 | OK (migrateV3ToV4) |
| 멱등 (이미 v4) | 변경 0 | OK (storage_v4.test.js 검증) |
| 부분 LEGACY (history만) | 부분 이전 정합 | OK |

## 2.5. 시나리오 7.18 라인업별 가변성

| 항목 | 드래곤볼 | 원피스 | 정합 |
|---|---|---|---|
| 등급 수 | 11 (A~J + Last One) | 10 (A~I + Last One) | OK (lineup.tiers.length 동적) |
| 박스 매수 | 80 | 80 | OK |
| DC winnersTotal | 50 | 100 | OK (lineup.dc.winnersTotal) |
| 자산 path | the_chronicle_of_goku_placeholder | monkey_d_luffy_placeholder | OK (lineup.assetsBasePath) |
| assetsAvailable | false | false | OK (양 라인업 placeholder 미배치) |

## 2.6. 시나리오 5.7.1 결정론 (M3 lineup_id 포함)

| 항목 | 검증 | 정합 |
|---|---|---|
| 동일 시드 + 동일 라인업 + 동일 회차 → 동일 box.id | boxId(seed, round, lineup.id) | OK |
| 동일 시드 + 다른 라인업 → 다른 box.id | lineup.id 포함 보장 | OK (lineup_isolation.test.js) |
| drawRng 격리 (5.7.5) | 박스 단위 fresh PRNG `createRng(fnv1a("seed|round|drawIndex"))` | OK (M2.1 정합 유지) |

# 3. 단위 테스트 커버리지

| Suite | 영역 | 신규/갱신 |
|---|---|---|
| `storage_v4.test.js` | v3→v4 마이그레이션 멱등 + 라인업 격리 + 전역 키 보존 | M3 신설 (T21) |
| `lineup_isolation.test.js` | box.id lineup_id 정합 + tierCounts(lineup) + LINEUPS / getLineupById fallback | M3 신설 (T21) |
| `box.test.js` | initBox 시그니처 + box.id 형식 | LINEUP_DRAGONBALL alias |
| `draw.test.js` | drawOne 결정론 + 등급별 매수 정합 | LINEUP_DRAGONBALL alias |
| `draw_pick.test.js` | drawOne pickIndex 분기 (M2.1) | LINEUP_DRAGONBALL alias |
| `last_one.test.js` | lastOnePrize | LINEUP_DRAGONBALL alias |
| `double_chance.test.js` | drawDc(tickets, rng, dcConfig) 신 시그니처 | M3 갱신 |
| `history.test.js` | tierCounts(history, lineup) 신 시그니처 | M3 갱신 |
| `storage_v3.test.js` | v2→v3 마이그레이션 (M2.1 호환) | SCHEMA_VERSION 4 정합 |
| `storage.test.js` | 기본 라운드트립 | 호환 |
| `build_consumed_grid_set.test.js` | core/pick-grid.js 단일 진실원 (M2.1 4.15.5) | import 경로 + lineup 인자 갱신 |
| 기타 | random / hash / buy | 정합 |

# 4. 라이브 시각 검증 (사용자 영역)

본 단계 7은 정식 시각 검증 단계. 사용자 라이브 컨펌 영역:

4.1. 헤더 라인업 IP 라벨 ("DRAGONBALL" / "ONE PIECE") 시각.
4.2. 설정 탭 'Lineup' dropdown 동작 + 확인 모달 + 라인업 전환.
4.3. 양 라인업 SVG fallback 표시 (assetsAvailable: false).
4.4. 라인업 전환 후 데이터 격리 + 복귀 시 보존 시각.
4.5. 9등급 라인업(원피스)의 minor-row / hero-carousel / product-gallery 정합.
4.6. DC winnersTotal 라인업별 차이 (50 vs 100) UI 표시.

# 5. 잔여 결함 (단계 8 백로그)

5.1. **assets.js 라인업 분기 (4.1 회색지대)**: lineup.assetsAvailable 무시. M3.1 또는 M4 별도 plan으로 흡수.
5.2. **사용자 외부 작업**: placeholder 자산 7장 (드래곤볼) + 10장 (원피스) 외부 도구 생성 + 폴더 배치.
5.3. **M3 단계 6 round 1 P2 3건**: tests alias / assets / T19 (이미 흡수됨).

# 6. QA 통과 판정

6.1. 정적 시나리오 정합: 6.1~6.6 / 7.17 / 7.18 모두 통과.
6.2. 단위 테스트 커버리지: 신규 2 suite + 기존 11 suite 정합.
6.3. 단계 6 게이트 통과 (P0 0건).
6.4. 라이브 시각 검증: 사용자 핸드오프 (단계 8에서 사용자 컨펌 후 종료).

**단계 7 QA 통과 (정적 정합 영역)**. 단계 8 improve 진입 + 사용자 라이브 검증 영역 이월.

# 7. 변경 이력

7.1. 2026-05-08: 단계 7 QA 작성. 단계 6 통과 후 정적 시나리오 + 단위 테스트 정합 보고. 라이브 시각 검증은 사용자 핸드오프 영역 명시.
