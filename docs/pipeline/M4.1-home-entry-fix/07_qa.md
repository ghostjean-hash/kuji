# M4.1 home-entry-fix - 07 QA

| 항목 | 값 |
|---|---|
| 사이클 ID | M4.1-home-entry-fix |
| 단계 | 7 QA |
| 작성일 | 2026-05-10 |
| 선행 단계 | 단계 6 impl_review round 1 통과 (P0=0 / P1=2 / P2=3, M4.2-tidy 흡수) |
| 자비스 정적 정합 | 통과 |
| 사용자 라이브 검수 | 의무 (본 보고서 4절 항목) |

# 1. 자비스 정적 정합 검증

## 1.1. arch 5.20 게이트 정합

| 검증 항목 | 결과 |
|---|---|
| `grep "STATE_VIEW_*" /Users/ghostjin/kuji/src` (코드) | ✅ 0건 (주석 박제만) |
| `grep "state\.view\b" /Users/ghostjin/kuji/src` (코드) | ✅ 0건 (주석 박제만) |
| numbers.js STATE_VIEW_* 4종 export 폐기 | ✅ Node ESM `m.STATE_VIEW_HOME === undefined` 확인 |
| numbers.js STATE_TAB_HOME = "home" / STATE_TAB_VALUES 4탭 / STATE_TAB_DEFAULT = HOME | ✅ |
| numbers.js SCHEMA_VERSION = 7 | ✅ |
| numbers.js DISPATCH_TYPE_OPEN_HOME / ENTER_LINEUP / SET_ACTIVE_TAB | ✅ |
| numbers.js TAB_ICON_IDS.home = "icon-home" 신설 | ✅ |
| storage.js migrateV6ToV7 export 정합 | ✅ |
| storage.js chain v3→v7 (5종 마이그레이션 순차) 정합 | ✅ |
| storage.js loadGlobalSettings activeTab 역직렬화 + STATE_TAB_VALUES 검증 | ✅ |
| storage.js saveGlobalSettings activeTab 직렬화 + invalid throw | ✅ |
| storage.js GLOBAL_KEYS.activeTab + LEGACY_VIEW_KEY_M4 | ✅ |
| main.js state.view 키 폐기 + activeTab 단일 라우팅 4탭 분기 | ✅ |
| main.js dispatch.open_home / enter_lineup / set_active_tab 의미 갱신 | ✅ |
| main.js bootstrapState activeTab 영속 복원 + STATE_TAB_VALUES 검증 | ✅ |
| bottom-tabs.js 4탭 환원 (홈 = 탭 1) | ✅ |
| home.js isCurrent = lineup.id === currentLineupId 단독 | ✅ |
| home.js renderHome 헤더 / sub 폐기 (main.js 공통 렌더 정합) | ✅ |
| header.js `<button>` → `<span>` + addEventListener 폐기 + aria-label 폐기 | ✅ |
| settings-tab.js dispatch.open_home 잔존 (의미 갱신) | ✅ |

## 1.2. Node ESM dynamic import 시뮬

```
[numbers.js]
  STATE_TAB_HOME = "home"
  STATE_TAB_DRAW = "draw"
  STATE_TAB_VALUES = ["home","draw","products_history","settings"]
  STATE_TAB_DEFAULT = "home"
  STATE_VIEW_HOME = undefined
  STATE_VIEW_MAIN = undefined
  STATE_VIEW_VALUES = undefined
  STATE_VIEW_DEFAULT = undefined
  SCHEMA_VERSION = 7
  DISPATCH_TYPE_OPEN_HOME = "open_home"
  DISPATCH_TYPE_ENTER_LINEUP = "enter_lineup"
  DISPATCH_TYPE_SET_ACTIVE_TAB = "set_active_tab"
  TAB_ICON_IDS = {"home":"icon-home","draw":"icon-draw","products_history":"icon-history","settings":"icon-settings"}

[storage.js]
  migrateV6ToV7 / migrateV5ToV6 / loadGlobalSettings / saveGlobalSettings / loadState / saveState / clearAll: function
  v6→v7 migrate (empty): {"migrated":true,"reason":"v6_to_v7"}
  schema_version: 7
  v6→v7 migrate (v6 fixture): {"migrated":true,"reason":"v6_to_v7"}
  active_tab preserved: draw
  home_acked preserved: true
  kuji_view removed: null
  v7 idempotent: {"migrated":false,"reason":"already_v7"}
  chain v3→v7 final schema: 7
  chain v3→v7 home_acked: true
[ALL ASSERTIONS OK]
```

## 1.3. 단위 테스트 신설 / 갱신

| 파일 | 케이스 수 | 비고 |
|---|---|---|
| storage_v7.test.js | 8 (빈 / v6 fixture / kuji_view 비표준 / v7 멱등 / 의미 변경 / chain v3→v7 / chain v5→v7) | T8 신설 |
| home_flow.test.js | 갱신 (M4 자산 흡수) | T9 |
| tab_routing.test.js | 7+ (STATE_VIEW 폐기 / 4탭 enum / dispatch / set_active_tab 검증) | T10 신설 (구 state_view 자산 흡수) |

## 1.4. 단계 6 impl_review 결과 흡수

| 결함 | 분류 | 처리 |
|---|---|---|
| P1-1 면책 모달 trigger 키 SSOT 충돌 (spec = homeAcked / 코드 = meta.disclaimerSeen) | P1 | spec 본문 stale. 단계 8 흡수 또는 M4.2-tidy 후보 |
| P1-2 arch 5.20 + impl_plan T9 단위 테스트 파일명 stale (home_flow_m41 vs home_flow) | P1 | docs 본문 stale. 단계 8 흡수 |
| P2-1 ~ P2-3 | P2 | M4.2-tidy 백로그 |

# 2. 사용자 라이브 검수 의무 (M4.1 단독)

브라우저에서 `tests/test.html` 실행 + 직접 시뮬레이터 진입 후 검수.

## 2.1. 첫 방문자 시나리오

2.1.1. **빈 storage 상태 (개발자도구 → Application → localStorage 비우기)** → 시뮬레이터 진입.
2.1.2. 면책 모달 노출 → dismiss.
2.1.3. **홈 탭 자동 활성** (= 라인업 카드 그리드 표시).
2.1.4. 헤더 = "이찌방쿠지 드래곤볼 ..." 타이틀 + "DRAGONBALL" IP 라벨 (클릭 affordance 부재 = `<span>` 정합).
2.1.5. 하단 탭 = 4탭 (홈 / 추첨 / 갤러리·기록 / 설정). 홈 active 시각.
2.1.6. 라인업 카드 클릭 → 추첨 탭 자동 활성 (= activeTab=draw).

## 2.2. 재방문자 시나리오 (M4 → M4.1 핵심 차이)

2.2.1. localStorage `kuji_home_acked=true` + `kuji_active_tab=draw` 상태에서 새로고침.
2.2.2. **홈 탭 자동 활성 (M4.1 신정책)**. M4까지의 "마지막 라인업 main view 자동 진입"은 폐기.
2.2.3. 사용자 발화 "기본적으로 진입하면 쿠지 홈이 있어야" 정합 확인.

## 2.3. 홈 ↔ 본편 탭 전환

2.3.1. 본편 탭(추첨/갤러리·기록/설정)에서 하단 "홈" 탭 클릭 → 홈 탭 활성.
2.3.2. 설정 탭 → "홈으로" 버튼 클릭 → 홈 탭 활성.
2.3.3. **헤더 IP 라벨 클릭 affordance 부재** (M4 정책 폐기). 라벨 = 표시 전용. 호버 시 cursor pointer 부재 정합.

## 2.4. 라인업 전환

2.4.1. 홈 탭에서 다른 라인업 카드(원피스) 클릭 → 추첨 탭 자동 활성 + 라인업 전환.
2.4.2. 헤더 IP 라벨이 "ONE PIECE"로 갱신.
2.4.3. 박스 / 인벤토리 / 이력 / DC 모두 라인업별 격리 보존 (M3 정합).

## 2.5. storage 마이그레이션 (브라우저 + DevTools)

2.5.1. **v6 fixture**: localStorage에 `kuji_schema_version=6` + `kuji_home_acked=true` + `kuji_active_tab=draw` 설정 후 새로고침. → schema=7 + active_tab 보존.
2.5.2. **v6 fixture (kuji_view 비표준 영속)**: localStorage에 `kuji_view=main` 설정 후 새로고침. → kuji_view 키 안전 제거.
2.5.3. **v5 fixture**: localStorage에 `kuji_schema_version=5` + `kuji_lobby_acked=true` 설정 후 새로고침. → chain v5→v6→v7 적용 후 home_acked=true / lobby_acked 키 부재.
2.5.4. 인벤토리 / 박스 / 이력 데이터 손실 0건.

## 2.6. M3 series 누적 라이브 검수 (M3.1/M3.2/M3.3/M3.5)

본 사이클 단계 7 QA에서 누적 흡수 (PROGRESS 11.4.4).

| 사이클 | 누적 검수 항목 |
|---|---|
| M3.1 | lobby UI / 카드 그리드 / hero 미리보기 |
| M3.2 | tier-class 시각 / hero 모션 / DC 모달 hero |
| M3.3 | 갤러리 그룹화 / history 대시보드 / 무한 스크롤 |
| M3.5 | hero-carousel / minor-row 분기 식 |
| M4 | 3탭 / 카드 메타 풍부화 / DC sub-section / "홈으로" 라벨 |
| M4.1 | 본 보고서 2.1~2.5 |

# 3. 단계 8 improve 진입 가능 여부

3.1. 자비스 정적 정합 = ✅ 통과 (arch 5.20 게이트 + Node ESM 시뮬 + grep 매트릭스).
3.2. 사용자 라이브 검수 = 의무 (사용자 액션 영역). 검수 결함 발견 시 보정 사이클(M4.2-tidy 흡수 또는 별도 보정).
3.3. **단계 8 진입 가능**. 학습 박제 + PROGRESS 갱신은 본 사이클 종료.

# 4. 비목표 박제

4.1. 라이브 검수는 사용자 액션. 자비스 단독 통과 결정 금지 (CLAUDE.md 7.2).
4.2. M3 series 누적 검수 결과 보정 = 별도 사이클 (사용자 결과 의존).
4.3. M4 P1-3 (3.18 set_current_lineup) / M4.1 P1-1~P1-2 / P2-1~P2-3 = M4.2-tidy 백로그.
