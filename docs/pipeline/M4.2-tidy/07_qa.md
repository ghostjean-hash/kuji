# M4.2-tidy - 07 QA

| 항목 | 값 |
|---|---|
| 사이클 ID | M4.2-tidy |
| 단계 | 7 QA |
| 작성일 | 2026-05-10 |
| 선행 단계 | 단계 6 impl_review round 1 통과 (P0=0 / P1=0 / P2=2) |
| 자비스 정적 정합 | 통과 |
| 사용자 라이브 검수 | 의무 (M4.1과 동일 + 정리 라운드 회귀 검증) |

# 1. 자비스 정적 정합 검증

## 1.1. arch 5.20 게이트 + impl_plan 8절 grep 매트릭스

| 검증 | 결과 |
|---|---|
| `grep -rn "from.*lobby\b\|from.*history-tab\b\|from.*dc-tab\b\|from.*tier-grid\b" src/` | ✅ 0건 |
| `grep -rn "tier === \"Last One\"\|tier !== \"Last One\"\|\"Last One\" in " src/` | ✅ 0건 (분기 식) |
| `grep -rn "tier-grid" src/` | ✅ 0건 |
| `grep -n "tier-grid" styles/main.css` | ✅ 0건 (박제 주석 1건만 잔존, P2-2) |
| dead 8 파일 fs 부재 (`ls`) | ✅ |
| Node ESM `LAST_ONE_TIER_NAME = "Last One"` | ✅ |
| Node ESM `STATE_TAB_HOME = "home" / SCHEMA_VERSION = 7` (M4.1 chain 보존) | ✅ |
| storage 마이그레이션 변경 0 (v7 chain 보존) | ✅ |
| 단위 테스트 변경 0 + dead 3 폐기 후 runner.js 정합 | ✅ |

## 1.2. 코드 동작 변경 0 정합

본 사이클 = 정리 라운드 의미. 다음 항목 변경 0 의무:
- 게임 메커닉 (박스 / 추첨 / Last One / DC) → 변경 0.
- 라우팅 (4탭 / dispatch) → 변경 0.
- storage 마이그레이션 / 영속 정합 → 변경 0 (v7 chain 보존).
- 단위 테스트 → 변경 0 (T1 dead 폐기 외).
- UI 시각 → 변경 0 (LAST_ONE_TIER_NAME 경유는 동일 문자열).

## 1.3. 단계 6 P2 흡수 결정

| 결함 | 처리 |
|---|---|
| P2-1 PROGRESS 13.2.5 카운트 표기 모호 | 단계 8 흡수 권고 (또는 백로그) |
| P2-2 styles/main.css 박제 주석 잔존 | 잔존 정합 (정리 라운드 박제 자체) |

# 2. 사용자 라이브 검수 의무 (M4.2-tidy 단독 + M4.1 누적)

## 2.1. 정리 라운드 회귀 검증 (M4.2 단독)

`tests/test.html` 브라우저 실행 + 시뮬레이터 진입 후:

2.1.1. **dead alias 폐기 영향 회귀**: 모든 탭 (홈 / 추첨 / 갤러리·기록 / 설정) 정상 동작. 콘솔 에러 0건. import 에러 0건.
2.1.2. **Last One 단일화 회귀**:
- 박스 deck 잔여 1매 시점 = Last One indicator pulsing 정상.
- 마지막 추첨 시 Last One 자동 지급 + 결과 reveal에 "Last One" 라벨 + is-last-one 클래스 적용.
- product-gallery에서 Last One = hero 그룹 마지막 위치 (M3.3 정합).
- product-detail-modal에서 Last One = "Last One" 표시 (라벨 보존).
2.1.3. **tier-grid 폐기 영향 회귀**: 갤러리 그룹화 시각 정합 (`.product-gallery-section*` 셀렉터만 적용). 시각 회귀 0건.
2.1.4. **storage 마이그레이션 회귀**: localStorage v7 fixture 그대로 부팅 → 정상 진입.

## 2.2. M4.1 누적 검수 (07_qa 2.1~2.5 답습)

본 사이클은 코드 동작 변경 0이라 M4.1 검수 결과가 그대로 유효. 사용자 액션 시 M4.1과 M4.2 동시 검수 가능.

| 누적 검수 | M4.1 / M4.2 |
|---|---|
| 첫 방문 / 면책 dismiss / 홈 탭 자동 | M4.1 + M4.2 회귀 검증 |
| 재방문 시 홈 탭 자동 | M4.1 + M4.2 회귀 |
| 라인업 카드 클릭 → 추첨 탭 자동 | M4.1 + M4.2 회귀 |
| 헤더 IP 라벨 클릭 affordance 부재 | M4.1 + M4.2 회귀 |
| storage v6→v7 마이그레이션 | M4.1 + M4.2 회귀 |
| M3 series 누적 (M3.1/M3.2/M3.3/M3.5) | 별도 |

# 3. 단계 8 improve 진입 가능 여부

3.1. 자비스 정적 정합 통과 + P0/P1 0건.
3.2. 사용자 라이브 검수 = 사용자 액션 (자비스 단독 통과 결정 금지).
3.3. **단계 8 진입 가능**. 학습 박제 + PROGRESS 13절 보강 (P2-1 카운트 표기 정정).

# 4. 비목표 박제

4.1. M3 series 라이브 검수 결과 보정 = 별도 사이클.
4.2. M5 = 코토부키야쿠지 30연 천장 룰 = 별도 메이저 사이클.
4.3. lobbyHeroAssetPath → homeHeroAssetPath 키 개명 = M5 흡수 또는 별도.
4.4. "Last One" 데이터 정의 / 표시 라벨 / 자산 키 단일화 = M5+ 별도 (부피 대).
