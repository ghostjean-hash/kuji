# 잘못 생성된 빈 폴더 (자비스 절대경로 처리 사고)
D:claude_codekujiresearch/

# OS
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Node / 빌드 산출물 (현재 프로젝트는 빌드 X지만 안전상)
node_modules/
dist/
build/

# 로그
*.log
npm-debug.log*

# 클로드 코드 사용자 로컬 설정 (권한 룰에 머신 절대경로가 적재되므로 settings.json도 제외)
.claude/settings.json
.claude/settings.local.json

# 라인업 참고 자료 (라이선스 0 정책 - CLAUDE.md 4.6 / 5.13.1. 추후 SVG 자체 제작 참고용 로컬 보관)
the_chronicle_of_goku_img/

# 신 자비스 v6.0.6 handoff JSON instance (사양 §3.5.7 unversioned 룰)
# template은 ~/.claude/workflows/<stage>-template.md (versioned, 자비스 본체)
# instance는 작업 디렉토리에 작성, M-jarvis-v6-migrate M3 시범 사이클부터 적용
**/*-handoff.json
**/*-handoff*.json

# contract.json instance (사양 §6.3.1 Default-FAIL, plan-template.md line 36)
# template은 ~/.claude/workflows/contract-template.json (versioned)
# instance는 작업 디렉토리, unversioned. acceptance_criteria 결과는 PROGRESS에 박힘
contract.json
**/contract.json
