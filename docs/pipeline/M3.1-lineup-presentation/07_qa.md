# M3.1 lineup-presentation 단계 7 QA

작성일: 2026-05-08.
검증 방식: 정적 시나리오 정합 + 단위 테스트 커버리지 + 사용자 라이브 시각 검증 의무 분리.
단계 6 round 1 통과 (P0 0 / P1 0 / P2 3) 결과 기반.

# 1. QA 목적

라인업 진입 로비 + tier_class 분류가 사용자 시나리오 (01_spec 4장 / 5.13.A / 5.13.B) + design_review 이월 결정 + 결정론 / 회귀 위험 0을 정합하게 처리하는지 검증.

# 2. 자비스 정적 검증 (코드/문서 grep + 단위 테스트 커버리지)

## 2.1. 시나리오 5.13.B.3.1 첫 방문자 자동 진입

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| 빈 storage 부팅 | loadState → migrateV4ToV5 → lobbyAcked=false | OK (storage.js line 235-236 + tests/storage_v5 "빈 storage 첫 방문" PASS) |
| bootstrapState | view = STATE_VIEW_LOBBY | OK (main.js line 580 분기) |
| rerender | renderLobby 호출 + 4탭/헤더 미렌더 | OK (main.js line 163-167 early return) |
| 카드 그리드 | LINEUPS 순회 + isCurrent=false (lobbyAcked === false 조건) | OK (lobby.js line 33) |
| 사용자 검수 의무 | 시각 (placeholder gray + IP 라벨) | **사용자 라이브 검수 (4장)** |

## 2.2. 시나리오 5.13.B.3.2 재방문자 자동 진입

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| v4 fixture (currentLineupId 존재) | migrateV4ToV5 → lobbyAcked=true | OK (tests/storage_v5 "v4 fixture currentLineupId 존재" PASS) |
| bootstrapState | view = STATE_VIEW_MAIN | OK (main.js line 580 분기) |
| rerender | 본편 4탭 모델 노출 | OK (main.js line 168-181) |

## 2.3. 시나리오 5.13.B.6.2 enter_lineup 분기 A (동일 라인업)

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| state.view = main | view 전환만 | OK (main.js line 421-428) |
| state.lobbyAcked 갱신 (false → true) | lobbyAcked === false 시만 | OK (line 423-426) |
| 메모리 only state 보존 | pendingPeelResult / selectedGridIndices 보존 | OK (분기 A 폐기 코드 0건) |

## 2.4. 시나리오 5.13.B.6.2 enter_lineup 분기 B (다른 라인업)

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| persist 현재 라인업 | saveState 호출 | OK (line 430) |
| currentLineupId + lobbyAcked 영속 | saveState({...}) 객체 인자 | OK (line 432) |
| bootstrapState 재구성 | loadStateForLineup(newLineupId) | OK (line 433) |
| 메모리 only 폐기 | pendingPeelResult/selectedGridIndices 초기화 | OK (bootstrapState line 575-577) |
| view = main | 명시 강제 | OK (line 434) |

## 2.5. 시나리오 5.13.B.5 보조 진입 경로 (헤더 라벨 / 설정 탭 버튼)

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| 헤더 IP 라벨 클릭 | dispatch.open_lobby | OK (header.js line 21-23) |
| 설정 탭 "라인업 선택 화면으로" | dispatch.open_lobby | OK (settings-tab.js line 42-49) |
| open_lobby | view = lobby + 메모리 보존 | OK (main.js line 406-412) |
| 사용자 검수 의무 | 시각 affordance (꺾쇠 아이콘) | **사용자 라이브 검수 (4장)** |

## 2.6. 시나리오 5.13.A.4 set_current_lineup quick-switch (M3 보존)

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| 설정 탭 dropdown 변경 | dispatch.set_current_lineup | OK (settings-tab.js line 27-31) |
| 확인 모달 | "라인업을 ... 전환합니다. ... 보존됩니다. ... 폐기됩니다." | OK (main.js line 462-466) |
| 라인업 전환 | bootstrapState 재구성 | OK (M3 동작 보존) |
| view 영향 0 | main 유지 | OK (set_current_lineup 분기에 view 변경 코드 0건) |
| lobbyAcked 영향 0 | true 유지 | OK (set_current_lineup 분기에 lobbyAcked 변경 코드 0건) |

## 2.7. 시나리오 1.4.A.3 tier_class 검증식

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| numbers.js 부팅 | _validateLineupTierClass 호출 | OK (numbers.js line 323) |
| 드래곤볼 hero/main/goods 각 ≥ 1 | hero=2, main=5, goods=4 | OK (tests/tier_class PASS) |
| 원피스 hero/main/goods 각 ≥ 1 | hero=2, main=5, goods=3 | OK (tests/tier_class PASS) |
| DC.tierClass = hero | 모든 라인업 | OK (tests/tier_class PASS) |
| 위반 시 throw | TIER_CLASS_VALUES 외 값 / 클래스 부재 | OK (numbers.js line 301-321) |

## 2.8. 시나리오 5.13.B.4.3 heroPreview 산출

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| 드래곤볼 heroPreview | A상 (Last One 제외) | OK (tests/lobby-preview PASS) |
| 원피스 heroPreview | A상 魂豪示像 (Last One 제외) | OK (tests/lobby-preview PASS) |
| 결정론 (동일 입력 → 동일 출력) | PRNG 의존 0 | OK (tests/lobby-preview PASS) |
| 빈 hero 배열 → null | 가드 정합 | OK (tests/lobby-preview PASS) |

## 2.9. 시나리오 storage v5 마이그레이션 멱등성

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| 빈 storage | lobbyAcked=false + schemaVersion=5 | OK (tests/storage_v5 PASS) |
| v4 fixture | lobbyAcked=true 추론 | OK (tests/storage_v5 PASS) |
| v5 fixture (이미 v5) | 멱등 (변경 0) | OK (tests/storage_v5 PASS) |
| lobby_acked 키 이미 존재 + schemaVersion < 5 | schemaVersion만 갱신 | OK (tests/storage_v5 PASS) |
| 직렬화 string "true" / "false" | localStorage 검증 | OK (tests/storage_v5 PASS) |
| 역직렬화 boolean | === "true" 비교 | OK (tests/storage_v5 PASS) |

## 2.10. 시나리오 라인업 격리 + 복귀 (M3 회귀 방지)

| 단계 | 기대 | 자비스 검증 |
|---|---|---|
| 드래곤볼 → 원피스 → 드래곤볼 복귀 | 박스/이력 보존 | OK (tests/lobby_flow "라인업 격리 + 복귀" PASS) |
| 라인업별 storage 키 격리 | kuji_history_${lineup_id} 패턴 | OK (M3 정합 보존) |

# 3. 단위 테스트 커버리지

| Suite | 케이스 | 상태 |
|---|---|---|
| tier_class.test.js (T7) | 12건 (TIER_CLASS_VALUES + 라인업당 분류 + DC + heroPreview) | 정적 정합 |
| storage_v5.test.js (T8) | 11건 (빈 storage / v4 / 멱등 / 직렬화 / 역직렬화 / loadState chain / SCHEMA_VERSION 상수) | 정적 정합 |
| lobby_flow.test.js (T9) | 9건 (view 모델 + 첫 방문 / 재방문 / 분기 A/B / 라인업 격리 복귀) | 정적 정합 |

**자비스가 코드 정합으로 통과 추정. 사용자 브라우저 ALL PASS 검증 의무 (4장)**.

# 4. 사용자 라이브 검수 의무 항목

다음 항목은 자비스 정적 검증 불가. 사용자 브라우저 라이브 검수 필수.

| # | 항목 | 절차 |
|---|---|---|
| 4.1 | tests/test.html ALL PASS | localhost 정적 서버 띄운 후 `tests/test.html` 진입. 19개 suite 모두 fail 0 확인 |
| 4.2 | 첫 방문 시각 (placeholder gray + IP 라벨) | localStorage 비움 후 새로고침 → 면책 dismiss → 로비 노출. 카드 시각 검수 |
| 4.3 | 카드 6요소 시각 검수 | hero placeholder + 한국어 제목 + IP 라벨 + 메타 한 줄 + heroPreview + CTA 모두 노출 |
| 4.4 | "현재" 배지 미노출 (첫 방문) | lobbyAcked=false 시 모든 카드 isCurrent=false → 배지 0건 |
| 4.5 | 카드 클릭 → main view 진입 | 시각 전환 + 다음 새로고침 시 로비 미노출 |
| 4.6 | 헤더 IP 라벨 클릭 시각 | 꺾쇠 아이콘 (›) 노출 + 호버 색 전환 + 클릭 → 로비 |
| 4.7 | 분기 A reveal 보존 | 추첨 진행 중 헤더 클릭 → 같은 라인업 카드 → reveal 그대로 |
| 4.8 | 분기 B 라인업 전환 | 다른 라인업 카드 → 새 라인업 공간 |
| 4.9 | 설정 탭 dropdown vs 버튼 | 둘 다 동작. dropdown은 main 유지 / 버튼은 로비 |
| 4.10 | 768px 반응형 | 윈도우 폭 변경 시 1열 ↔ 2열 전환 |

# 5. 결함 / 권고

5.1. 단계 6 P2 3건 (storage_v5 v3 chain 명시 / "main" 인라인 / "Last One" 인라인)은 단계 8 백로그 등재. 단계 7 통과 차단 사유 아님.

5.2. **자비스 결함 발견 0건**. 사용자 라이브 검수 결함 발견 시 단계 7 round 2 진입 또는 단계 5 재진입.

# 6. 통과 판단

6.1. 자비스 정적 시나리오 정합 (2장) + 단위 테스트 커버리지 (3장) 모두 통과.
6.2. 사용자 라이브 검수 (4장) **별도 의무**. 검수 결과 결함 0건 보고 시 최종 통과.
6.3. 4.1 (tests/test.html ALL PASS) 가 단계 7 통과의 핵심 게이트.

# 7. 변경 이력

7.1. 2026-05-08: 초기 작성. 자비스 정적 검증 통과 + 사용자 라이브 검수 의무 명시.
