# M4 단계 7 QA

작성일: 2026-05-10. 자비스 자율 진행 ("권고 진행" 신호 답습).

# 1. 자비스 정적 검증 통과 (단계 6 round 2 결과 답습)

1.1. SSOT 정합 (02_data ↔ numbers ↔ spec ↔ arch ↔ render 모듈).
1.2. dispatch 호출 매트릭스 정합 (open_home / enter_lineup / set_active_tab + 폐기 호출처 0).
1.3. storage v5 → v6 마이그레이션 멱등 + chain 정합.
1.4. 단위 테스트 11 suite / 58 test ALL PASS (Node ESM 시뮬).
1.5. CLAUDE.md 4장 절대 규칙 위반 0.

# 2. 자비스 정적 시나리오 (예상 동작)

| # | 시나리오 | 예상 결과 |
|---|---|---|
| 2.1 | 첫 부팅 (빈 storage) | home_acked=false → state.view=home → 홈 카드 그리드 노출 |
| 2.2 | v5 fixture (lobby_acked=true 잔존) | v6 마이그레이션 자동 → home_acked=true + lobby_acked 키 제거 → main view 자동 진입 |
| 2.3 | 홈에서 라인업 카드 클릭 | dispatch.enter_lineup → state.view=main + 추첨 탭 진입 |
| 2.4 | main에서 헤더 IP 라벨 클릭 | dispatch.open_home → state.view=home + 헤더 IP 라벨 미렌더 |
| 2.5 | 하단 탭 = 추첨 / 갤러리·기록 / 설정 (3탭) | bottom-tabs 3 카드 |
| 2.6 | 갤러리·기록 탭 진입 | 4 sub-section: 대시보드 (4 카운터) / 갤러리 그룹 (hero/main/goods) / history 리스트 / DC 응모 |
| 2.7 | 설정 탭 진입 | dropdown 부재 + "홈으로" 버튼 + 시드/박스/통/면책/초기화 잔존 |
| 2.8 | 홈 카드 메타 표시 | 출시일 + 끝일 + 가격 + 박스 매수 + 추정 배지 + 매장 (3개+ → "N곳") + 진행 상태 (박스 회차 / 추첨 / DC) |
| 2.9 | 미진행 라인업 카드 진행 상태 | "아직 시작 안 함" 표기 |
| 2.10 | 다른 라인업 카드 클릭 (분기 B) | 메모리 only state 폐기 + 새 라인업 공간 로드 |

# 3. 사용자 라이브 검수 의무 (브라우저)

## 3.1. M4 단독 검수 (10건)

3.1.1. 첫 부팅 시 홈 view 자동 진입 (헤더 IP 라벨 미렌더 / 라인업 카드 그리드 노출).
3.1.2. 홈 카드 메타 풍부화 (출시일 + 끝일 + 가격 + 박스 + 매장 + 진행 상태).
3.1.3. 카드 클릭 (드래곤볼) → main view 진입 (헤더 IP 라벨 노출 + 추첨 탭 default).
3.1.4. 하단 탭 = 3탭 (추첨 / 갤러리·기록 / 설정).
3.1.5. 갤러리·기록 탭 = 대시보드 + 갤러리 그룹 + history 리스트 + DC 응모 4 sub-section.
3.1.6. 설정 탭 = dropdown 부재 + "홈으로" 버튼 + 기존 시드/박스/통/면책/초기화.
3.1.7. 헤더 IP 라벨 클릭 → 홈 복귀 (메모리 보존).
3.1.8. 다른 라인업 카드 클릭 → 메모리 only state 폐기 (pendingPeelResult / selectedGridIndices).
3.1.9. 동일 라인업 카드 클릭 → 메모리 보존.
3.1.10. 라인업별 진행 상태 정합 (추첨 후 카드 진행 표시 갱신).

## 3.2. M3 series 누적 검수 (사용자 결정 9.5)

3.2.1. M3.1 라인업 로비 (현 홈) 카드 hero 미리보기 + IP 라벨 + 메타.
3.2.2. M3.2 추첨 탭 hero-carousel/minor-row 액센트 + 결과 reveal hero 모션 + DC 모달.
3.2.3. M3.3 갤러리 그룹화 + history 대시보드 (현 통합 탭에서 확인).
3.2.4. M3.5 원피스 B/C/D/E/F = hero 분류 (carousel + minor-row + 갤러리 + 대시보드 모두 정합).

## 3.3. 마이그레이션 검수 (M4 신설)

3.3.1. M3.5 사용자가 v6 빌드 진입 시 lobby_acked 키 → home_acked 키 자동 이전 + lobby_acked 키 제거.
3.3.2. 활성 라인업 / 박스 / 인벤토리 / history / DC 모두 보존.
3.3.3. 새로고침 후 동일 흐름 재현.

# 4. 결함 보고 흐름

4.1. 사용자 라이브 검수 후 결함 발견 시 본 문서 4장에 결함 항목 추가.
4.2. 결함 0 보고 시 단계 8 improve 정식 종료.
4.3. M4 결함 + M3 series 누적 결함 모두 차기 정리 라운드(M4.1-tidy)에서 일괄 흡수.

# 5. 변경 이력

5.1. 2026-05-10: 초기 작성. 자비스 정적 통과 + 사용자 라이브 검수 의무 (M4 10건 + M3 series 4건 + 마이그레이션 3건).
